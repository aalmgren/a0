import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginBottom: 15,
  },
  progressText: {
    fontSize: 16,
    marginBottom: 5,
    fontWeight: '500',
  },
});
